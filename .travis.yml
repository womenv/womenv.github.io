language: node_js
node_js:
  - lts/*
install:
  - yarn install # npm ci
script:
  - npm run build # npm run docs:build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: src/.vuepress/dist
  github_token: $GITHUB 
  keep_history: true
  on:
    branch: gh-pages
